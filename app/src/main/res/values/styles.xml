<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.Base" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="buttonStyle">@style/button_style</item>
    </style>

    <style name="button_style" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/colorSecondary</item>
    </style>

    <style name="MyDatePicker">
        <!-- Text color -->
        <item name="android:textColorPrimary">@color/black_500</item>
        <!-- Divider color -->
        <item name="colorControlNormal">@color/purple_200</item>
    </style>

    <style name="ProgressTheme">
        <item name="colorAccent">@color/purple_200</item>
    </style>

    <style name="MyToast">
        <item name="stTextBold">true</item>
        <item name="stTextColor">@color/white</item>
        <item name="stFont">@font/roboto_slab</item>
        <item name="stTextSize">14sp</item>
        <item name="stColorBackground">@color/purple_700</item>
        <item name="stSolidBackground">true</item>
        <item name="stStrokeWidth">3dp</item>
        <item name="stStrokeColor">@color/purple_200</item>
        <item name="stLength">LONG</item>
        <item name="stRadius">5dp</item>
    </style>

    <style name="AlertDialogueTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

</resources>